apiVersion: v1
kind: Service
metadata:
  name: players-v1-service
  labels:
    app: players-v1
spec:
  type: ClusterIP
  selector:
    app: players-v1
  ports:
    - port: 8000     # 8000 flask 8080 fastapi
      targetPort: 8000     # 8000 flask 8080 fastapi
      name: http
      protocol: TCP
  
